# WeedOut
WeedOut is an A-CPS weed management system that avoids data-hungry CNNs by classifying plants using crop shape and size rather than visual features. It works across growth stages and regions, needs no prior training, and clusters similar plants for semi-supervised labeling using minimal farmer input.
